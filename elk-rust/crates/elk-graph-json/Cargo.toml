[package]
name = "elk-graph-json"
edition = "2024"
license = "MIT"
rust-version = "1.85"
version = "0.1.0"

[dependencies]
elk-core = { path = "../elk-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
elk-layered = { path = "../elk-layered" }

