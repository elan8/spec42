//! SysML explicit view catalog, evaluation, and projection.
//!
//! The canonical implementation lives in [`semantic_core::semantic::explicit_views`]; this
//! module keeps the historical `crate::views::explicit_views` path for the LSP kernel.

pub use semantic_core::semantic::explicit_views::*;
