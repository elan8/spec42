package BaselineConfiguration {
    doc /* Default system configuration. */

    import SystemDefinitions::*;

    part baseline : System {
        doc /* Baseline configuration of the system. */
    }
}
