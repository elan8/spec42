[package]
name = "spec42-zed"
version = "0.20.0"
edition = "2021"
description = "Zed extension for SysML v2 language support via the spec42 LSP"
license = "MIT"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

[workspace]
