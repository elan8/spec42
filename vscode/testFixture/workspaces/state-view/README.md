# State Machine Sample

Open this workspace when you want a compact behavior/state-oriented example.

Highlights:
- simple event-driven timer state machine
- `state def`, `transition`, `first`, `accept`, and `then` patterns
- good fit for cold-start highlighting and state transition visualization checks

Primary file:
- `StateMachineDemo.sysml`
