# sequence-view fixture

Minimal `SequenceView` over an `occurrence def` scenario: typed `part` lifelines
with ordered `event occurrence`s, `message … from … to …`, and `succession` for
message order. Used by `diagram_generator_smoke` to check that the sequence
projection carries participants, message edges, and ordering.
